/**
 * This package contains the REST controllers for the user service application.
 *
 * <p>The controllers in this package handle HTTP requests and delegate
 * business logic to the appropriate service classes. They are responsible
 * for processing incoming REST API requests, invoking the necessary services,
 * and returning appropriate responses.
 *
 * <p>The main controller in this package is:
 * <ul>
 *   <li>{@link com.example.userservice.controller.UserController}: Manages
 *   CRUD operations for User entities.</li>
 * </ul>
 */
package com.example.userservice.controller;
